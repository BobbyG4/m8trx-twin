---
description: Start a session in m8trx-twin (digital-twin sister project; segregated from core)
---

Read these files to get oriented:

1. **`/Users/bob/IdeaProjects/m8trx-twin/CLAUDE.md`** — project context, system-integrator posture, layered generator architecture, tenant model, off-limits, requirement-flow protocol.

2. **`/Users/bob/IdeaProjects/m8trx-twin/status/STATUS.md`** — the **`## ⚠ NEXT SESSION PRIORITIES`** section is the AUTHORITATIVE source for "Today's options". Do not invent options outside of it.

3. **`/Users/bob/IdeaProjects/m8trx-twin/status/tracks/TRACK-TWIN.md`** — current branch state, blocked items, open work, active requirements filed to core.

4. **`/Users/bob/IdeaProjects/m8trx-twin/status/SESSION-LOG.md`** — rolling summary + session index with links to session-notes. For deep detail on a past session, read the linked session-notes file.

Optional, only if relevant to today's task:
- `/Users/bob/IdeaProjects/m8trx-twin/reference/architecture/LAYER4-CONFIG-SCHEMA.md` — scenario config schema (the architectural commitment)
- `/Users/bob/IdeaProjects/m8trx-shared/twin/SISTER-PROJECT.md` — twin↔core relationship, brief-filing protocol (only load when filing a brief or absorbing one)

**Do NOT read m8trx-shared core files** (`m8trx-shared/CLAUDE.md`, `m8trx-shared/status/STATUS.md`, etc.). Twin sessions stay segregated from core context. The only m8trx-shared path twin sessions ever touch is `m8trx-shared/twin/`.

---

Summarize in this exact format:

## Session Start — [today's date]

**Where we left off:**
[2-3 sentences from TRACK-TWIN.md + latest SESSION-LOG rolling summary entry]

**Blocked on core:**
[items from TRACK-TWIN.md "Blocked on Core" section — check if any have since landed]

**Active requirements filed to core:**
[from TRACK-TWIN.md, status only for non-ABSORBED items]

**Today's options:**
[3-5 concrete next actions ranked by priority, from STATUS.md `## ⚠ NEXT SESSION PRIORITIES`]

Keep it tight — no more than 25 lines total. Bob picks a task and starts immediately.

**If sources disagree, STATUS.md wins.**
