# Track: Twin

**Last session:** Session 17 (2026-07-31) · ✅ **CLOSED.** **★ ZERO-LOSS CONFIRMED — oracle 1512 / wire 1512 / rows 1512 / sessions 308 / fixtures 115-115 / 0.00%**, on the reproducing profile (seed 4242, hours 12-16, compress 18). The external proof the impression-loss fix never had, closing the arc open since S15 — and it settled the S282-vs-twin contradiction as a **timeline** rather than a side: 9.4% pre-fix → **20.3% at 03:03** → **total** during the `vision_ai` entitlement-gate outage → **0.00% post-#245**. Built the **§6.5 READ consumer** (`ReadPlane.kt` · 4 `ConnectClient` methods · `connectReadProbe` · `impressionVerify`) and **CLOSED TWIN-REQ-004** as external prover. Built **`connectAcceptance`, the Connect ship gate** (15/15). Fixed **F4** (three-phase episode → core's `min()` discriminates) and **exonerated it by A/B** when persistence went to zero. **Session notes:** [→](../session-notes/2026-07-31-session-17-connect-read-half-ship-gate-zero-loss.md)

---

## ★ STANDING ROLE — twin IS the Connect regression suite (Bob's ruling, 2026-07-31)

**M8TRX's paid, externally-sold API surface has no automated regression coverage.** Every CI security suite drives a human JWT against frontEnd RLS or greps source text; **not one drives a Connect API key against a Connect endpoint.** Every Connect defect to date was found by twin driving it from outside, or by a human reading a screenshot — never by a gate.

**Until a CI deploy gate with live keys exists, `./gradlew connectAcceptance` IS the gate.** This is a standing responsibility, not a one-off:

- **Green before ship**, owned, not incidental.
- **Extend it as the surface grows.** Alarm ingest is the seventh inbound data-type and is already listed in the task's `NOT_COVERED` literal awaiting its envelope. A new data-type gets a row in `COVERED` or `NOT_COVERED` — *"we forgot"* stops being expressible.
- **Coverage gaps are as load-bearing as failures.** Both lists print on every run; a silently-skipped endpoint is the false-green class this exists to prevent.
- **The omitted-site assertion must stay non-vacuous.** It proves the key is confined (finds a refused site) *before* comparing the omitted-site set — because *"omitted returns everything I can see"* is trivially true for an unconfined key and would green on a total leak. No refusal anywhere ⇒ **INDETERMINATE, never PASS**. Core is adding the server-side half; **if the two ever disagree, that disagreement is the finding.**

⚠ **Needs a site-scoped key to be meaningful.** Twin's `M8TRX_TWIN_BEARER` is Denver-scoped, which is what makes the negative controls real.

### ★ AND the cold-onboarding peer test is the second standing gate (Bob, 2026-07-31)

Bob's stated goal is **self-service integration** — he named API integration as *"the most friction on any project, and we always seem to have to customize."*

**The peer test is the only instrument that detects the thing which forces that customization: onboarding that requires a human on M8TRX's side.** Everyone building a surface holds the access that hides the problem — they can register a source, mint a scope, load a mapping, and never notice the vendor cannot. **Twin cannot, and that is the whole value.**

**How to run it — the standard does not move:** author the field-map and payload **cold, from the published contract, asking nothing.** *If you need to ask how, that is the finding.* §8.1 invites exactly this report, and the first run of it produced `/alerts/query`, the §8.2 registration section, and a corrected read-back claim inside an hour.

**Report one of three verdicts, and keep them distinct — they are different outcomes:**
- **(a)** author and send unaided, end to end.
- **(b)** author everything; onboarding needs a human, **as documented**. ✅ An honest contract with a known limitation — this is a PASS. The original defect was a doc promising self-serve and delivering a wall.
- **(c)** something does not match the doc. ⛔ The failure case.

**Judge the doc against reality, not against the ideal.** The test twin found most useful: *"does the contract correctly predict what happens to someone following it?"* — not *"does it promise the right things."* Most docs fail the first.

⏳ **Sequencing (Bob):** the cold run happens **after** the alarm plumbing is complete and confirmed, never against half-built plumbing — that measures the missing half, not whether a stranger can onboard. **Wait for the coordinator's signal.** Last verdict: **(b)**, 2026-07-31, with one (c) on the capability route (see TWIN-REQ-005).

---

**Prev session:** Session 15 (2026-07-28) · ✅ **CLOSED.** **The people pipeline is PROVEN end to end, camera-free, at full-day volume with the oracle EXACT.** Full day: **1,100,584 samples · 790 shoppers · 44m20s · 3,664 impressions vs 3,664 predicted (0.0%)** · 97 distinct fixtures · 790/790 shoppers produced impressions · median dwell 60.0s · tag `fullday-0728`. Single episodes **DB-confirmed 5/5**. **First circle-fixture impression ever** (`PI-01` → `7dc6fb79`) = the acceptance gate for Connect's `GeometryConverter` fix, which the same run showed is **half-complete** (`Geometry.Circle.edges()` is a stub → **containment-only proximity**; a shopper 0.6m from a promo island accumulates no dwell). Built: `runtime/` (locked Q2/Q3/Q6), `layer1/{FixtureGeometry,ImpressionOracle,BrowseEpisode,ZoneAffinity,PeopleDrive}`, `layer2/Journeys`, `layer3/{OperatingModel,TrafficGenerator,Reconciliation,ScenarioRun,DayDrive}`; tasks `peopleSelfTest` (49) · `scenarioSelfTest` (23) · `connectPeopleDrive` · `connectDayDrive`. **✅ MERGED 2026-07-30 as PR [#13](https://github.com/BobbyG4/m8trx-twin/pull/13) — `main` now `39c8238`** (was `b0390fc`; branch `chore/spine-restart-hygiene`, 15 commits). **Session notes:** [→](../session-notes/2026-07-28-session-15-spine-restart-people-pipeline-full-day.md)
**Prev-session detail (S15 mid-flight, retained):** **BRIEF-TWIN-SPINE restart.** Ground-truthed both paired coordinator briefs against real code before building (Bob's instruction; corrections accepted into m8trx-shared through `9f8f68e0`). **Caught 2 brief errors that would have burned both parallel sessions:** (1) **`viewDirection` is mandatory** at `XovisImpressionEvaluator.kt:311` — twin never emitted it, so every people-event would have been silently dropped with `impression_event` staying at 0 and each session blaming the other; the real killer is the **>1 Hz emit floor** (below it every gap exceeds the 1000ms allowance, both clocks reset per sample, and `millisTillImpression` is unreachable however long the shopper stands there); (2) **Connect outbound already exists** (`OutboundWebhookDispatcher` + `OutboundRetryJob`, live-proven by twin in S11) — the Connect brief said it did not, and would have had that session rebuild working infrastructure. Also surfaced: the **edge-server instance was unowned across all three briefs** (→ ruled to the Connect session), and **`crossing` is a dead end in v2** (`sliceId` is a v1 `arealayoutslice` concept; v2 has `crossing_line`/`zone_crossing`; only `onCrossing` implementor is `Journaler.kt`; `crossing_line` shows the same never-written fingerprint as `impression_event`) → **descoped from 3.2**. *(Mid-flight this note claimed twin therefore could not drive footfall — **that was WRONG and was ruled 2026-07-28**: `person_session` IS the visit record, live and proven, and twin's own S15 episodes created six. The §1 reconciliation identity closes on it; `crossing_line` is a post-MVP door-counter, not a prerequisite.)* **★ FR-PLN-08's Notifications-spine gate has CLEARED** — rule engine live on master `f14482a`; the owed **directive→task smoke is unblocked**, needing only a backend watcher (task read is still not `@ConnectExposed`, verified by enumerating every annotation site). **Shipped:** 3.1 spine live (`connectChainActivity`, paced ~1 event/6s at conc 1, ~530 sales clean); 3.4 hygiene (5 stale HOLD FIRE sites — not 2; `seed_store.py` de-hardcoded + marked SUPERSEDED, **secret rotation still owed, Bob's**); **`peopleSelfTest`** (38 at that point, **49 at close**) — impression-rule conformance harness (`layer1/`: `FixtureGeometry` · `ImpressionOracle` + `explainSilence()` · `BrowseEpisode` at 5 Hz) proving the emit contract offline, plus the **zone-affinity re-key** off `(space_type, zone_type)` + structural code (ruling decision 3) resolving in all 10 stores with zero unmapped. Incidental: **74% of Denver's sales floor lies within 1m of a fixture edge** (realism input — dwell is far less discriminating than §8 assumes). Branch `chore/spine-restart-hygiene`. Note: `com.m8trx.geometry` is off-limits (shared with Android AR) — twin wrote its own local planning geometry.

**Prev session:** Session 14 (2026-07-02→03) · ✅ **CORE-REQ-005 parts 1–3 done · TWIN-REQ-003 SATISFIED · ★ Strand V site-scope hardening GREEN.** **Part 1** — composed the built P0 drivers into ONE parameterized path-(b) loop (`FullLoopDriver` + `connectFullLoop`; **PR #9**): directive → sale-drift → `items/details` assert SOLD → movement/scan remediate → assert present → per-target compliance **expectation** (the session oracle, since `/state` 403s); **(fixture×SKU) grain**; dry-run proven (Denver `GB-R3-U1` 28 targets → **19/8/1**, under-stock + the #7/FR-COLLECT-ID case surfaced). **Part 2** — launch-quality stress harness (`StressHarness` + `connectStress`; **PR #10**): concurrent (coroutines+Semaphore) multi-arm sale storm (NoScope / store-xref / site-scoped) + dedup/unmapped probes + breakage report + sampled SOLD-verify. **TWIN-REQ-003 filed** (compliance/directive read-back over Connect; pushed to shared `118e495`). **Live stress (clear-to-hammer, ~810 sales, 3 waves):** conc **6 & 12 clean** (100% `200`, ~9–11/s, SOLD-verify 40/40 post-delay); conc **24 → ~50% `502`** (upstream choke, all arms + the Bearer read, **instant recovery**). **Safe ceiling ≤12; throughput plateau ~10/s; no `429` backpressure** — **reproduced S8 event-loop starvation from the public API** (backend deep-dive independently confirms; fix = `429`@nginx + offload/MVC, **post-demo, NOT demo-blocking**). Task-read **`403 CONNECT_NOT_EXPOSED`** (JWT-only, sibling to TWIN-REQ-003); Bearer confirmed LIVE. **Gotcha:** `.env` not auto-loaded by gradle `JavaExec` (+ stale daemon env) → live drives need `set -a; . ./.env; set +a` + `--no-daemon`. `main` clean (PRs #9 + #10 merged). **Then (07-03, same session):** **part-3** — core shipped `POST /api/v2/compliance/state` (PR #76) → twin fired live `200` · 28/28 · **25/2/1** · 0.893 → **TWIN-REQ-003 SATISFIED** (the `/state` 403 wall closed, assert-split done). Surfaced the **seat/perms tenant-wide leak** while provisioning demo logins (root = `seed_users.py` inserting `user_tenant_membership` per site-role — core's seed bug; twin roster verified clean). **★ Strand V site-scope hardening** — built **`connectSiteScopeAudit`** (public-surface+JWT confinement gate; **PR #11** `e390e0f`): baseline **24 RED → 0** post-hardening; a broad **151-table sweep caught 3 event/audit leaks core's core-36/tail-35 mapping missed** (`integration_event`/`item_custody_event`/`unified_audit_event`) → core fixed → twin re-verified **0**; write-plane confined (cross-site confused-deputy `403`; own-site `403` = correct capability limit). **main `2131384`** (PRs #9/#10/#11 merged).
**Prev session:** Session 13 (2026-07-01) · ✅ Live-compliance demo PROVEN end-to-end (drift compliant→partial→non_compliant + movement remediation); `connectMovementDrive`+`connectReceiveDrive`; #72 core bug fixed; clean-room SGTIN-96 decoder handed to core; PR #8 merged.
**Last session notes:** [→](../session-notes/2026-07-02-session-14-core-req-005-fullloop-compose-stress.md) · [→](../session-notes/2026-07-03-session-14-strand-v-sitescope-hardening.md)

> **Coordinator note (m8trx-shared S193, 2026-06-30):** a coordinator session (designs in `m8trx-shared/status/active/`, *not* a twin session — the phantom "Twin S12" was reverted) established that **planogram-via-Connect is MVP and the twin is the MVP external driver**. Future twin work includes building the **planogram-directive driver** (`directive_kind='planogram'` over Connect). See `m8trx-shared/status/active/{PLANOGRAM-RESOLVED-DESIGN,CONNECT-PLANOGRAM-MVP-SCOPE-CORRECTION}-2026-06-30.md`. Memory: [[project_mode3_planogram_connect_is_mvp]] (m8trx-shared namespace). **✅ BUILT + LIVE-PROVEN S12** — the planogram-directive driver fires `directive_kind='planogram'` → resolved compliance targets (PR #7, as-built ingest #64/#65/#66).

---

### ⚙ Drive run-card (kept here because S15's handoff is now archived)

```bash
set -a; . ./.env; set +a
export M8TRX_NATS_URL="nats://192.168.55.29:4223"        # 4223 = edge-twin-denver. NOT 4222 (office/real Xovis)
export M8TRX_SPACE_ID="e3c9a424-3ced-5288-9756-19935d39f88f"
export M8TRX_SITE_ID="84f2a1c1-fb0a-41b2-9e0d-c9102a22ca7e"
export M8TRX_DAY_DATE=2026-07-28 M8TRX_DAY_SEED=4242 M8TRX_DAY_SCALE=1.0 M8TRX_DAY_COMPRESS=18.0
export M8TRX_DAY_FROM_HOUR=12 M8TRX_DAY_TO_HOUR=16       # THE REPRODUCING PROFILE — 469,665 samples, 308 shoppers, 1512 predicted
export M8TRX_DAY_LIVE=true                                # omit for dry-run
./gradlew connectDayDrive --no-daemon                     # ~22 min wall
```

**The four-number protocol** (never collapse these — oracle-vs-rows alone cannot attribute a gap):
1. `./gradlew oracleDump` → **oracle** (the model, offline, before publishing)
2. `./gradlew impressionWatch` in the background → **wire** (core's own published output)
3. the drive → publishes
4. `./gradlew impressionVerify` over the drive's wall-clock window → **rows** (what persisted)

`M8TRX_EPISODE_APPROACH_MS=0 M8TRX_EPISODE_DISENGAGE_MS=0` reverts F4's three-phase episode to the old coincident-clock shape — that A/B is what exonerated F4 when persistence went to zero.

---

## Current State

**S16 (2026-07-30) — current:** `main` **`39c8238`** — **PR #13 merged**, the whole S15 spine (runtime/ · layer1/ · layer2/Journeys · layer3/) is now on `main`; no open PRs, working tree clean. Picked up `m8trx-shared/status/briefs/BRIEF-TWIN-LANE-2026-07-30.md` (p2, coordinator, nothing blocking). Ledger reconciled: **TWIN-REQ-004 → ABSORBED core-side** (2026-07-30, sequenced behind the Traffic reference surface — interim positions stay live); **CORE-REQ-001 was already ABSORBED** twin-side since 2026-06-21 (`eb39526`) in *both* STATUS and TRACK, so the coordinator's ledger flag is stale on their copy, not ours.
**★ `person_session` orphan close — NOT TWIN (§1 answered loudly).** The ~90 Denver orphans that vanished before the 2026-07-30 platform-wide open-stale count of 0 were **not closed by twin**, and twin cannot have closed them. Evidence: (a) **no twin code path closes a `person_session`** — the only `personSessionClose` in the repo is `layer0/RestEmitter.kt:36`, which has **zero callers** and no `AtomEmitters` implementor binding it; it is unwired Layer-0 scaffold; (b) direct psql is Off-Limits and twin has never held a mother DB credential; (c) **twin ran nothing after S15** — last real gradle execution `2026-07-28 15:57` (`.gradle/9.6.1/executionHistory/executionHistory.bin`); the only 07-30 timestamps are daemon lock/probe files and the PR-merge checkout; (d) the core-side backup dir the brief cites (`m8trx-shared/status/migrations/backups/2026-07-29-pre-person-session-close/`) was written **2026-07-29 18:20** and contains exactly one file — `orphans-office-2026-07-29.csv`, i.e. **office**, not Denver. **So someone else touched the Denver rows, and there is no backup CSV for them.** Twin's only `person_session` interaction is *implicit and legitimate*: `objLocation` samples + an `objEviction` per object over NATS on 2026-07-28 (`PeopleDrive.kt:209`, `DayDrive.kt:258` — the full day emitted **evictions 790**, one per shopper, so twin's own drives closed out cleanly at the NATS layer). If core closes `person_session` on eviction, twin leaves no orphans by construction; the remaining twin-side candidate would be a drive **killed before its eviction loop**, which would strand open sessions — but no twin run exists in the 07-29/07-30 window when the Denver rows changed. *(Twin-side convention adopted for any future close, per the brief: `exit_time = max(evidence)`, else `entry_time` as a **0-second stub** — never invent durations, always back up first.)*
**BW reference-surface run windows (§2):** Denver stream stays available for E1 hand-verification (`m8trx-shared/status/active/PLAN-CONVERGENCE-TO-SHIP-2026-07-28.md` §7 DoD); windows coordinated through Bob. **Standing rule on every further day-drive: compress the GAPS, never the EPISODES** — already implemented and invariant-guarded in `DayDrive.kt:92-167`.
**★ §1 STOOD DOWN — nobody touched the data (coordinator, 2026-07-30).** Mother Denver holds 985 sessions, **all closed organically on 07-28**: 0/907 carry any close-rule fingerprint, exits strictly follow last impressions, and the 4 honest 0-second stubs come from the 03:29–04:30 broken-token window twin's own `err.log` documents (224 begin-401s, 8 close-403s). Edge-local table empty by design. **The "~90 Denver orphans" was the coordinator's own unverified S277 claim and is RETRACTED** — no Denver backup was ever needed, and the dwell distribution plus every downstream oracle comparison are clean. Twin's evidence chain held; flagging loudly was the right call and cost nothing.
**★ Item 1 ANSWERED — the loss is 1,408, not 545.** The 3,664 was **fullday-only**, not a session total: twin's own S15 close records "Prefix `fullday-0728`, expect **3,664** rows" (§6.2), and the evening slice is separately tallied at 854. Against the coordinator's DB truth (3,119 Denver rows = fullday 2,256 + evening-peak 854 + tests 9): **fullday-0728 = 3,664 → 2,256 = 1,408 lost (38.4%)** · **evening-peak = 854 → 854 = 0 lost (0.0%)**. Sessions in the same fullday run: **790 published → 790 persisted (0%)**. Built `lossAudit` and **falsified two hypotheses** before they could waste core's time: (1) *concurrency/cache-capacity ceiling* — peak concurrent `(shopper,fixture)` entries is ~169 and is roughly **duration-independent** under arrival compression (peak = arrival rate × episode length), so a 4.3× volume gap does NOT imply 4.3× concurrency; (2) *row keyed on `(person_session,fixture)` so repeats UPDATE* — repeats are real but only **5.6%** of impressions, an order of magnitude short of 38%. **Hard fact for core:** `person_session` 790/790 vs `impression_event` 2,256/3,664 in the *same* stream, same edge, zero twin errors → the gap is on the **impression path, downstream of ingest, and not the rule** (whose output matched the oracle exactly). Investigation stays with the BW lane.
**★ Item 2 RECONCILED — premise inverted twice, and twin was the short one.** The 3 Promo Island 1 rows are twin's OWN early tests (`twin-circle-acceptance-2` 8.6s · `sec3-dod-drive` 22s · `sec3-poststrip` 22s, one impression each; the 8.40/8.53/8.51s twin quoted are their look-spans). **Nothing multiplied** — twin's S15 accounting ("can account for one") was short by two later §3 DoD drives it never tallied. Fullday contributed **ZERO** Promo rows, and **neither branch of the coordinator's decision tree applies: there was no fullday Promo shopper at all.** The circles never participated in the day — that is exactly the §6.1 department-keying gap, now fixed below — so twin's oracle correctly predicted 0 for `PI-01` in the fullday, and PI-01 is **not** a persistence-loss instance and does not feed item 1. The separate "oracle said 1 where reality was 0" is already recorded at S15 §3.3: it was the *circle standoff attempt* (radius+600mm, before the containment-only behaviour was understood), reported at the time and since encoded in the regression.
**★ Fixture coverage closed (S16):** 97/115 → **115/115** at full scale, two root causes (department-keyed pools + boxed-in fixtures vs a fixed standoff) — detail in Open Work #1. `peopleSelfTest` 49→**50**, `scenarioSelfTest` 23→**27**; all green incl. `ktlintCheck` · `compileKotlin` · `connectSelfTest`.
**Edge stack (§6):** `m8trx-twin-edge-server` predates five later GCP fixes (calibration plumbing twin does not use — twin generates its own coordinates). If current code is ever wanted before a demo run: one `docker compose up -d` in `/opt/m8trx/edge-twin/`. Not urgent.

**S14 (2026-07-02→03):** `main` **`2131384`** clean, PRs **#9** (`connectFullLoop`) + **#10** (`connectStress`) + **#11** (`connectSiteScopeAudit` — site-scope confinement gate) merged → **12 `connect*` drivers**. **CORE-REQ-005 parts 1–3 done** (full-loop + at-scale stress + compliance read-back smoke GREEN). **TWIN-REQ-003 SATISFIED** (core PR #76 `925f9a4` deployed — `POST /api/v2/compliance/state` live). **★ Strand V site-scope hardening GREEN** — `connectSiteScopeAudit` baseline 24 RED → 0; the at-scale **151-table sweep caught 3 event/audit leaks** core's core-36/tail-35 mapping missed → fixed → re-verified; writes confined. Live-stress ceiling (part-2): conc **≤12 clean**, **24 → ~50% `502`** = **S8 event-loop starvation** (fix `429`@nginx + offload/MVC, post-demo). **Seat/perms tenant-wide leak** surfaced (`seed_users.py` grants tenant-membership per site-role — core's fix). *(Detail below is prior accumulated state; the SHA/PR-count there predates #8–#11.)*

**m8trx-twin:** main `ae5fcf2` (PRs #2–**7** all merged — clean, no open PRs). **★ S12 — Planogram Mode 3 LIVE-PROVEN:** `build_planogram.py` (10× `planogram.json`; 84,266 floor units = the reseed split) + `PlanogramDirectiveDriver` + DTOs + `connectPlanogramDrive` (dry-run default; `connectSelfTest` 7 cases green). Drove the Denver `GB-R3-U1` slice (28 targets) → 200 ack → server-verified `compliance_directive a3b2bbde` + `_site` + 28 `_target`; resolver (#65/#66) flipped all 28 → `resolved`. ⚠ **Open:** the operator mapping pointed `GB-R3-U1` at the wrong zone ("Gondola R6 Front U1" = code `GF-R6-U1`, not the real "Gondola R3 Back U1") — (a)/(b) fix-path with Backend; load-bearing for a live-compliance demo. **Toolchain current (S10):** Gradle 9.6.1 · Kotlin 2.4.0 · jackson 2.21.4 · jnats 2.25.3 · coroutines 1.11.0 · logback 1.5.37 · ktlint 12.2.0 · JVM 21. **★ S11: the ENTIRE Connect surface is LIVE-exercised** — inbound (sales + catalog/pricing/shipment) · Bearer self-verify (closed loop, `items/details`) · outbound `stocktake_result` (C3: happy + retry/heal). **6 live `connect*` drivers** (`connectMultiSiteSmoke` · `connectSaleStream` +`.twin-state/` sold-EPC persistence · `connectChainActivity` ×10 stores · `connectSelfVerify` · `connectScanSweep` dry-run-default · `connectOutboundReceiver`); `connectSelfTest` green. Creds in gitignored `.env` (machine-local): `M8TRX_TWIN_BEARER` (working, 4 scopes) · `M8TRX_TWIN_WEBHOOK_KEY` · `M8TRX_CONNECT_OUTBOUND_VERIFY_SECRET`. ⚠ `twin-outbound` (`fcffa62d`) test integration lingers on M8trxDemo (admin-only delete — ask BACKEND).
**Chain dataset (Wave 1):** built + committed under `reference/data/chain/` — **14 sites** (10 retail + 4 office, all with lat/long), **251 users**, **2,586-SKU** catalog, **102,675 EPCs**, **10 UNIQUE per-store departmentalized layouts**, localized USD/EUR/KRW · EN/FR/KO. Deterministic builders (`build_layout`/`localize_names`/`build_chain`/`build_staff_roster`/`size_curve`/`sport_universe`/`render_floorplans`, sha256 seeds).
**Realism overhaul (Session 7, 2026-06-22, committed `17872e5` + `be0f712`):** (a) **sport-universe DEPARTMENTS** from `brand` — flagship 6–7 / large 4–5 / medium 2–3 (count = min(7 universes, gondola rows); Denver/SF 6, NYC/Paris 7) (`sport_universe.py`; `build_layout` emits department `region` bands replacing the single "Main Sales Floor"; `build_chain` places SKUs by department, absent universes → *General* in small stores); (b) **lean BACK-OF-HOUSE** — Stockroom (Z-05) = real `receiving_dock` + `backroom_rack`; 18% of each style staged; (c) **realistic SIZE CURVES** (`size_curve.py`) — per-style bell, color-aware; fixes the flat-depth "88-pair shoe" Bob caught; **277,515 → 102,675 EPCs** (old total was the bug; density knob `TIER_SCALE` ~2× = testing variety + realism); (d) **SITE GEO** — lat/long on 14 sites (was 0/14 on mother).
**★ Spatial hierarchy corrected → `site → spaces → zones` (Pass 1, committed `c480446`):** the single-space build was the error — a site has MANY spaces (ruling `m8trx-shared/reference/dev/SPATIAL-HIERARCHY.md`, grounded in `7a. Data Model`). Each store now = **3 spaces** (Sales Floor `sales_floor` / Back Room `stockroom` / Fitting Rooms `fitting_room`), each its own SRF; **departments are `region` zones *in* the Sales Floor** (not spaces). `layout.json`→`spaces[]`, manifest→`stores[].spaces[]`; `assortment`/`epcs` unchanged (fixture resolves via `spaces[].zones[]`). Pass-1 = structure (assembly cols `srf_to_site_transform`/`site_frame_anchor_space` DORMANT); **Pass 2 = site assembly (transforms + `space_connection`) PENDING.** `space_type` provisional pending Backend/Web ratification.
**Layout (per-store, parametric):** footprint/grid/aisles/specialty/departments/BOH seeded off store_id (flagship ~675m² → medium ~370m²), 0 overlaps + 0 OOB asserted; mother-canonical zone geometry. SVGs (departments tinted, BOH shaded) via `render_floorplans.py`. *(Earlier 2026-06-22 work also fixed the original 134-overlap bug — twin source, not core's seed.)*
**Catalog coding layer (CORE-REQ-001):** `assortment.csv` +`brand` +`classification_key` +`department`; `classification.csv` (5 roots + 90 leaves, `attributes_schema`, `lifecycle_type`) + `display_lookup.csv` (colour raw→canonical family ×3 locales, swatch). Decathlon normalisation model. **Absorbed in core**; applied to the demo tenant by the reseed.
**MK/Hansae coding profile (built 2026-06-21):** the numeric-code model — `reference/data/mk-trend/` (`display_lookup.csv` 344, `classification.csv` 10 div + 63 items, `assortment-sample.csv` 2,610 SKUs round-tripping 100%) from the real MK Trend spec (`reference/hansaemk/`, ex-Zenven). Same grain as Decathlon → proves attribute-coding is vertical-portable. Config/parser `scripts/mk_coding.py` + `build_mk_attributes.py`; writeup `reference/data/mk-trend/MK-CODING-PROFILE.md`.
**M8trxDemo on mother:** ✅ **RE-RESEED v2 (landed + verified 2026-06-26)** — `tenant_id ecfa6903-5c50-439f-8f80-185982de944e`; canonical `site → spaces → zones` live: **30 spaces** (10 stores × `sales_floor`/`stockroom`/`fitting_room`, own SRF each) · **929 zones** · **53 try-on** · **102,675 items** (84,266 floor / 18,409 BOH 17.9%, dual-written `thing_location`+`scan_event`) · catalog coding live (2,586 products / 95 classes / 2,586 images, USD) · geo + `site_category` on 14 sites · `space_type` Hasura-exposed · Hasura `is_consistent:true`. Pre-seed backup retained on mother.
**Reseed:** ✅ **DONE + VERIFIED 2026-06-26** (RE-RESEED v2; hand-off `DEPLOY-HANDOFF.md` §RESEED-2026-06-22 executed by core). Twin-side cross-check vs the committed dataset passed **byte-for-byte** (9/9 headline metrics — spaces, zones, try-on, items, floor/BOH split, products, classes, Denver per-space) → **zero drift, no dataset amendment needed.** Recorded core-side at m8trx-shared `693f706`; Hasura `is_consistent:true`.
**Static-seed audit (Session 8):** structural seed complete + verified; remaining gaps — (a) data present but unapplied: **staff/org model** unprovisioned (250-person roster/roles/reporting in `staff/`), **per-region currency** (mother USD-only — single shared US master), **localized names** (EN-only); (b) thin/absent substrate: **sensor/reader topology** (only 2 stubs, 0 in BOH/fitting), empty **try-on profiles** (`properties={}`), absent **LP/EAS substrate** (0 watch SKUs, no demo zone, no EAS tag); (c) blocked-on-core (images, service-bearer, commerce_projection). Full detail in session-notes.
**★ Integration pivot (Session 8):** future seeds **and** active interactions route through **M8TRX Connect** (public webhook/HMAC front door), with parallel ERP/external simulators injecting `ACTIVITY-PLAN` activities. Bob authoring the Connect **API doc** (shares next session). Supersedes direct-DB seeding as the path — also sidesteps the bulk-reseed audit cascade + the service-bearer-inventory blocker. Posture win (front door).
**★ M8TRX Connect P0 simulators (Session 9, 2026-06-27 — BUILT + offline-verified):** new Kotlin package `com.m8trx.twin.connect` (CORE-REQ-003) — foundation (`ConnectConfig` · `Hmac` · `http/{ConnectHttp,ConnectError}` typed `ConnectResponse` where non-2xx is data · `ConnectClient` Bearer gateway · `WebhookClient` · two `ObjectMapper`s for the camel/snake casing split · DTOs built to the **code-verified** Connect §8 shapes) + all 5 P0 simulators (`setup/ApiKeyBootstrap` · `sim/InboundPushDriver` · `sim/DeviceDriver` · `sim/OutboundReceiver` · `setup/Provisioner` + `sim/SftpDropDriver` CSV formatter). `./gradlew connectSelfTest` green (HMAC sign↔verify · DTO casing round-trips · full OutboundReceiver loop accept/dedupe/tamper-reject/missing-sig/failMode). **SFTP sshj transport deferred** (Bob's scope call; formatter done). Added `.editorconfig` (intellij_idea/150 — the repo had none, so ktlint was never enforced; cosmetically reformatted 4 pre-existing files into compliance). Corrected `M8TRX-API-SURFACE.md` rows #22–24 (superseded by Connect §8). **LIVE-VALIDATED (S9):** inbound `sale_event` (EPC + SKU) → PROCESSED against `twin-pos` (tenant `ecfa6903…`, integration `5dfba5cd`); and once a **Bearer service key** landed (issued **out-of-band** — the `twin-pos` X-API-Key is webhook-ONLY, 401s on all `/api/v2`), twin **self-verified `state=sold`** (2 Denver EPCs `…21C51C`/`…5756B1` SOLD, control in_stock) + all 3 scopes (`inventory:read` · `scan:submit` /scans 202 · `integration:manage` /health 200). **Full loop proven** (twin → Connect webhook → core moves inventory → twin reads it back SOLD). ⚠ **Bearer key NOT persisted** (protocol: no creds in repo) — re-supply next session to `.env` as `M8TRX_TWIN_SERVICE_BEARER`. 10 mother site UUIDs stashed at `reference/data/chain/site_ids.csv` (§6 device driver needs real site UUIDs — data-plane, not webhook-translated). twin↔core coordination via async mailbox `m8trx-shared/brainstorm/COMMS-CONNECT-TWIN-2026-06-27.md` (append-only, no creds in-file). Guides: `reference/connect/{SIMULATOR-GUIDE,LIVE-OPERATIONS}.md`.
**EPC encoder:** VALIDATED (`EPC-ENCODING-DECATHLON.md`) — filter 1 / partition 6, round-trips real tags.

## Open hand-offs
- **`reference/data/chain/DEPLOY-HANDOFF.md`** — **rewritten 2026-06-22** as the authoritative RESEED hand-off (§RESEED-2026-06-22: in-place mechanism, deploy order, acceptance checks). 2026-06-11 kept as history.
- **`reference/data/chain/EXPANSION-PLAN.md`** — Wave 2 (10 international stores, varied layouts) — folds in the onboarding-baseline thread (`status/active/ONBOARDING-BASELINE-HANDOFF-2026-06-03.md`).
- **`reference/data/chain/ACTIVITY-PLAN.md`** — Phase-2 dynamic layer + Connect simulator + reset-to-opening-state.
- **`reference/data/chain/SEED-PLAYBOOK.md`** — reusable recipe + the 5 backend corrections (read before re-seeding).

## Open Work — top of queue (S18)

1. **⚠ TWO UNMERGED BRANCHES STACKED.** `fix/fixture-coverage-in-area-zone` (S16, 7 commits, no PR — that session died before close) with `feat/connect-read-half` (S17, 7 commits) on top. **PR S16's first.**
2. **Restore `integration:manage` + grant `alert:read`** on `M8TRX_TWIN_BEARER` — one admin action. Unblocks FR-INTEG-16, the §7 DLQ, and the `/alerts/query` half of the EAS test. **Twin cannot self-serve this** (SEC-1 subset guard + `CONNECT_NOT_EXPOSED`).
3. **LP chain end to end**, once BW-TRIAD applies mig 205 and BW-CONNECT registers `twin-eas`: alarm → `alert` → routed to an LP role → visible on `/alerts` → dispositioned, **each hop verified**. ⚠ **Twin owes two pieces of substrate first** — see 6.
4. **FR-INTEG-16 — feasibility ANSWERED: YES** (see ledger). Building it is gated behind FR-INTEG-01's scope hierarchy, not behind twin.
5. **directive→task follow-up** — a *decision*, not build work: expose a scoped activation, or declare the loop deliberately human-gated in the middle. Twin can drive the input and read the output; it cannot cause the middle.
6. ✅ **DONE 2026-07-31 — LP substrate BUILT.** `layer2/EasTagging.kt` (the rule `price >= $150 AND category != "outdoor"` → **271 of 2,586**, tag state declared **twin-side**) + the **`Shoplift` journey** in `Journeys.kt` (browse → conceal → skip checkout → track crosses `CS-01` at `y=600`; `EasGateCrossed` published **from the emitted samples**, so the alarm is a consequence of the track, not fired alongside it). Dead "W-series watches" anchor removed from `CLAUDE.md`, `STATUS.md` and `store-operating-model.json`, with its **absence asserted** by `scenarioSelfTest` (27 → **37**). A generated day fires 2 concealments / 2 alarms. *(Was:)* **Twin's own LP substrate gaps** — **zero watch SKUs** in the live 2,586-SKU catalog (the documented "W-series watches, EAS-tagged, 40 items" anchor belongs to the superseded 920-SKU concept; Session 8 flagged it and it was never closed), **no per-item EAS tag column**, and **`Shoplift` is a design-table entry, not code** (absent from `Journeys.kt`). Gates exist and are real (`CS-01`, 10/10 stores, real SRF geometry).
7. **F4's end-divergence is not closed.** `firstDwell` → `firstLook` diverge by the approach phase on a persisted row (proven live), but `lastDwell == lastLook` because the impression flushes at eviction before the disengage tail lands. **Core's `min()` still cannot be observed selecting between two different arms** — needs an episode that outlives its own flush.
8. **Planogram grain** — `build_planogram.py` is `item_cd`-keyed in two places, so `5391035`'s target sums both sizes and takes whichever EAN was read last. Fix identified (`(fixture, ean)`), **not applied**: it regenerates 10 documents and changes directive payloads. Needs a go.
9. **§5 economics recalibration** (unchanged) — ATV $58 / avg line $26.40 are from the superseded 871-SKU catalog. Bob's decision.
10. **⚠ STILL OWED (Bob):** rotate the mother Hasura admin secret — de-hardcoding did not remove it from git history.

## Open Work — historical (S16)

> **★ ALL SIX S15 RULINGS ARE CLOSED.** Nothing in the spine is blocked on a decision. The items below are twin's own work plus verification owed elsewhere.

1. ✅ **DONE 2026-07-30 — fixture coverage 97/115 → 115/115.** *(Was: 18 never browsed, incl. all three circles.)* The dig found **two independent root causes**:
   - **(a) Fixture pools were keyed off `department`.** 19 of Denver's 115 fixtures carry none — 6 GPS cases (`Z-06`), 3 accessories bays (`Z-07`), gait treadmill (`Z-09`), footwear bench (`Z-08`), 4 checkout counters + impulse rack (`Z-02`), and the 3 circles `PI-01`/`PI-03`/`RR-02` (`Z-01`). Re-keyed onto **`in_area_zone`**, validated present/non-null/orphan-free on every fixture in all 10 stores with band-department agreement wherever a department exists — so it subsumes the old map rather than sitting beside it. Added **`ZoneAffinityModel.fixtureBrowseRate(role)`** — P(stops at a rack | in the zone), distinct from P(enters the zone) — because without it zone-keying overshoots and puts all 790 shoppers at the three entrance circles for a full zone median. All values `[A-new]`, surfaced by `uncalibratedBrowseRates`.
   - **(b) `ACC-02` and `GPS-04` were unreachable BY CONSTRUCTION** — not sampling thinness. Both are boxed in on all four sides with 200–400mm gaps (`ACC-01`/`ACC-03` at 200mm, `GPS-04`/`GA-01` at 400mm) against a **fixed 600mm standoff**, so every candidate point landed *inside* a neighbour, no candidate ray-validated, and the fallback aimed the shopper at the neighbour — impressions landing on `ACC-01`/`GPS-02`. Same wrong-fixture class as the S15 paired-gondola bug, arrived at from the opposite direction. `standoffPoint` now fits the standoff to each edge's clearance (`min(600, clearance/2)`; edges under `MIN_CLEARANCE_MM=150` dropped, since a flush neighbour means no standing position exists; jitter scaled to `0.4 × standoff` so drift can't cross either surface). **Byte-identical to the proven behaviour wherever there is room** — clearance is capped at `2 × standoff`, so an open edge still yields exactly 600mm, and the paired-gondola case still passes. Standoff solving is memoized per `(fixture, standoff)` — deterministic and shopper-independent, so a day solves 115 times instead of ~10,000.
   - **Regression, deliberately split by what it can honestly test:** `peopleSelfTest` **[10] — all 115 visible fixtures individually browsable**, deterministic and scale-free (49 → **50** cases); `scenarioSelfTest` gets coverage %, the three circles by name, and the department-less class as a threshold (23 → **27** cases), because a coverage number over a generated day mixes unreachability with ordinary sampling and only the first is a bug.
   - **Full-scale day:** **115/115 covered** · 792 visitors · **1,177,671 samples (+7.0% vs S15's 1,100,584)** · **3,967 predicted impressions (+8.3% vs 3,664)**. At the ~415 samples/s publisher bound that pushes a day-drive from 44m20s to roughly 47m. Default-scale (0.12) day covers 107/115 — sampling, not reachability.
   - ⚠ **NEW finding, flagged not fixed — layout realism.** 200–400mm gaps between 2–3m-deep display blocks are not walkable aisles; a real Decathlon would not build that. It is a **twin dataset** defect (`build_layout.py`), not a platform one. Deliberately left alone: regenerating layouts would break the byte-for-byte reseed parity with mother that RE-RESEED v2 verified. Worth a pass whenever layouts are next regenerated for another reason.
2. ✅ **DONE 2026-07-30 — DB verification delivered by the coordinator, and it found a real gap.** 3,119 Denver rows: fullday-0728 **2,256** (vs 3,664 over NATS → **1,408 / 38.4% lost**), evening-peak **854** (vs 854 → **0%**), tests 9. Twin answered the split (the 3,664 was fullday-only) and falsified two hypotheses; see Current State. **Core's investigation, filed with the BW lane — not twin's to chase.** Twin's residual: `⚠ STATUS/notes must stop saying "the oracle was EXACT" unqualified` — it was exact against the *emitted stream*, which was never a persistence claim.
3. **22,944-row Hasura truncation ceiling — still UNTESTED.** Note the DB total (3,119) is well under it, so the ceiling is **not** implicated in the 1,408 gap; it remains an untested *read-path* question.
4. ✅ **DONE 2026-07-30 — reconciled, and twin's accounting was the short side.** The 3 Promo Island 1 rows are twin's own early tests; fullday contributed zero because the circles never participated in the day (the department-keying gap, now fixed). No multiplication, no persistence loss, no oracle discrepancy. Detail in Current State.
5. **§5 economics recalibration (decision, not a drive-by).** ATV $58 / avg line $26.40 come from the superseded 871-SKU running-store catalog, not the live 2,586-SKU chain assortment (median $55, max $11,999). Same staleness class as the §8 zone codes. The sampler makes the generator obey the model *as written*; recalibrating §5 is Bob's call.
6. ✅ **DONE 2026-07-31 — TWIN-REQ-004 filed, shipped, proven, and CLOSED.** *(This line said "not yet written"; it was already filed 2026-07-28 and was stale even then.)* Twin proved the whole §6.5 read half from outside — confinement on all four reads, refusal semantics, and the paging contract — and closed the requirement. Detail in the ledger below and in `m8trx-shared/status/active/RESULTS-TWIN-2026-07-31.md`.
7. **directive→task smoke** (owed since S14) — **unblocked**, rule engine live on master `f14482a`. Needs a backend watcher (task read still not `@ConnectExposed`).
8. **Publishing throughput is the wall-time bound**, not the schedule (~415 samples/s). A larger day or lower compression binds on the publisher.

## Blocked on Core

- **`Geometry.Circle.edges()` is a stub → containment-only circle proximity (NEW, S15).** Connect's `GeometryConverter` fix landed (edge loads 115/115) and twin proved the first circle impression, but the dwell half is not at parity: the `dwellProximity` band contributes nothing for a circle, so a shopper 0.6m from a promo island is "not nearby". Proven live — that configuration emits `lookingAtFixture` but no `dwellingNearbyFixture` and no impression. Core documents it as a KNOWN GAP; the real fix (`distanceToBoundary` on `Geometry`) touches the artifact shared with **Android AR**, so it is a decision. Twin models containment-only and stands ON the footprint; regression asserts both directions so the assertion flips when core ships it.
- **★ RESOLVED S15 (kept for the record):** `viewDirection`-off gate ruling · edge-server ownership · space/zone UUID route (mother fixture map delivered) · footfall (`person_session` is the visit record; `crossing` descoped) · TWIN-REQ-004 scope · `impression_event` never-written (now written).
  - **Space-UUID resolution** — twin's `layout.json` carries `zone_id: null` throughout (mm geometry, no mother identifiers) and **no Connect-exposed endpoint returns space identity**: `items/details` returns only `epc/itemId/sku/attribs/state/imageUrl`, and `items/receive` *requires* a `spaceId` it never hands back. Twin recommends a **one-time hand-off** (precedent: S9's `site_ids.csv`) over minting a tenant-wide human reader, plus **TWIN-REQ-004** for a durable Connect spatial read (**unfiled, pending Bob**). Note the integrator-facing defect: a third party cannot call `receive` without someone emailing them UUIDs.
  - **NATS vs REST for the impression join point** — the Connect session made `ImpressionEventController` `@ConnectExposed(dataPlane=true)`, so a Bearer key can POST impressions directly. That removes the edge-server dependency but **bypasses `ImpressionStateMachine` entirely** (no geometry, no dwell clocks) — it proves the subscriber/service tail, not the pipeline. Twin's recommendation: both, with REST-injected rows tagged distinguishably so the first *computed* row stays a real landmark.
  - **Footfall / `crossing`** — no v2 consumer (see Current State). Until one exists, the operating-model reconciliation identity (`visitors → transactions → revenue`) cannot close platform-side; twin can drive dwell + transactions only.
- **~~FR-PLN-08 compliance-check task-gen gated on the Notifications spine~~** — ✅ **CLEARED (verified S15):** rule engine live on master `f14482a` (services PR #75). The **directive→task smoke** is unblocked; task read remains JWT-only (`TaskController` carries no `@ConnectExposed`), so run it **with backend watching**.
- **FR-COLLECT-ID / FR-INTEG-04 — receive product-linkage (NEW, S13)** — Connect item-receive is **EPC-only** (`ReceiveItem(epc)`, no product resolution) → a new-EPC receive lands `thing.product_id` NULL → compliance can't count it (#7's receive→relocate climb blocked). Root = the **Identifier Resolution Pipeline (FR-COLLECT-07..10)** isn't built. Twin **handed core the clean-room SGTIN-96 EPC↔EAN decoder** as the reference impl (`m8trx-shared/reference/dev/EPC-EAN-DECODER-FR-COLLECT-ID.md`); Backend logged it a priority (~30-line port + wire-into-receive), **NOT this round**. **Twin re-fires #7 when Backend pings it deployed.**
- **Compliance read-back — `GET /state` → 403 `CONNECT_NOT_EXPOSED` (S13)** — twin's Connect key can't self-verify compliance; drove the whole demo blind (loop closed via Backend DB reads). **Backend owns it** — folds into the Connect **read-surface** on the FR-INTEG rollout (a scoped `/state` read). Interim = DB-reads-via-Backend. (**Filed as TWIN-REQ-003**, 2026-07-02, per CORE-REQ-005 GO.)
- **Image pipeline (NEW, gating Wave-2 images)** — `assortment.csv` `image` = Shopify CDN hot-link; backend hit an issue; likely need to **cache bytes** into M8TRX's own asset store, not hot-link. Confirm what the seed did + whether core can store/serve cached assets. Part of `CATALOG-IMPORT-ONBOARDING`.
- **✅ Connect Bearer plane + OI-1 RESOLVED (S11)** — service-Bearer mint un-stubbed core-side (services #51/#52); a `principal_kind='service'` key with `scope_grants` now 200s on `/api/v2`. Self-serve **Keys-tab mint/rotate/revoke** shipped (services #53 / web #31, `POST /api/v2/connect/service-keys`) + verified consumer-side (mint→use→revoke). Twin self-verifies SOLD via `items/details`. (Was OI-1: no self-serve scoped-Bearer mint; `/api/v2/connect/credentials` `@MvpStub`.) Note: key-mgmt + `DELETE /integrations/{id}` are admin-JWT-only by design (`CONNECT_NOT_EXPOSED` to Connect keys).
- **NEW finding (S11) — dedup-shadows-failed-retry** (core CLEANUP, filed for Bob): a *failed* outbound event's content-hash blocks a same-payload retry + (not quarantined) escapes map-and-replay. The fix in the moment was re-firing with a different payload.
- **Connect `lookup` transform ↔ `integration_lookup` table (OI-2, NEW)** — the `lookup` field-transform reads the `integration_lookup` table, NOT the `value_lookups` JSON; no UI to load rows. Optional (EPC path needs no `site_id`; 10 site UUIDs stashed twin-side for the device driver). Channel **OI-2**.
- **Catalog onboarding flow incl. images** — no tenant product-import path. `CATALOG-IMPORT-ONBOARDING`.
- **commerce_projection writer** — unfed; commerce dashboards blank on API path. **TWIN-REQ-002** (filed).
- **No cold-start/manual location** — inventory location requires a scan/receive event (corrections §2). Core CLEANUP-TASKS.
- **No EAS-alarm subscriber** — LP/theft analytics don't surface (API-SURFACE gap).
- **Auth 500 / Hikari pool starvation (NEW, Session 8)** — post-reseed, the bulk-mutation Hasura **audit-trigger cascade** exhausted m8trx-services' `HikariPool-1` (10/10 active, 30 waiting) → auth/exchange 500 (reqId `3cc2943b`). Mother DB healthy (74/200); likely amplifier = the 102,675-item dual-write (~205k rows). **Core's to fix** (pool headroom / async-batch audit-ingest / quiesce triggers during bulk load) — **OPEN, core investigating.** Connect-based incremental seeding avoids the bulk direct-DB writes that trigger it. (Confirmed NOT caused by the twin session's read-only audit.)

## Open Work (priority order)

> ✅ **DONE 2026-06-26 (gating item cleared):** RE-RESEED v2 landed on mother + twin-side verification passed **byte-for-byte** (zero drift, no amendment). Core recorded it at m8trx-shared `693f706`. Static-seed gap audit delivered (see Current State + session-notes).

> **★ TOP PRIORITY (S12 → next) — Planogram Mode 3 demo loop.** Directive→targets→**resolved** is PROVEN (twin + Backend + COORD). **Next:** (i) settle the **wrong-zone mapping** with Backend — **(a)** twin sends vendor codes + supplies the code→zone map from `layout.json` (realistic, exercises the `fixture_code_mapping` path) / **(b)** twin sends zone *names* as `raw_fixture_code` → auto-resolve, no operator step (simpler) — load-bearing for any live demo; (ii) the **live-compliance demo** (correct mapping → activate `effective_date=today` → sale-stream tap → compliance moves), self-contained once the mapping's right; (iii) scale to the **full Denver** directive (2,504 targets); (iv) **FR-PLN-08 compliance-check task-gen — gated on the Notifications spine** (Backend, deferred critical path). The numbered backlog below is the prior Connect/runtime work (still valid).

1. **★ M8TRX Connect — finish P0 + start the runtime.** ✅ **S9:** all 5 P0 sims built + offline-verified; inbound webhook + Bearer plane **LIVE-validated** against `twin-pos` (`sale_event` → PROCESSED; **self-verified SOLD**; all 3 Bearer scopes). **Next (ranked):**
   - **(a) Re-supply the Bearer key** to `.env` as `M8TRX_TWIN_SERVICE_BEARER` (out-of-band; NOT in repo) — prereq for any `/api/v2` work. Env also needs `M8TRX_TENANT_ID=ecfa6903-5c50-439f-8f80-185982de944e`, `M8TRX_CONNECT_INTEGRATION_SLUG=twin-pos`, `M8TRX_TWIN_WEBHOOK_KEY=<twin-pos key>`.
   - **(b) §9 outbound receiver loop — the LAST unexercised P0 sim.** Stand up `OutboundReceiver` on this box's `192.168.55.x` LAN IP → provision an outbound `stocktake_result` channel via `Provisioner` → agree a shared `hmac_secret` + confirm dev→LAN egress (else tunnel) with BACKEND (ready to wire the trigger, channel msg-02) → verify sig+dedupe+2xx, then non-2xx → retry/poison/DLQ.
   - **(c) Harness hardening (solo):** `items/details` lookup in `ConnectClient` (SOLD-verify through twin code), a `DeviceDriver` runner, configurable `OutboundReceiver` bind address (binds `127.0.0.1` today).
   - **(d) Start the `LIVE-OPERATIONS.md` runtime** — per-site business-hours calendar (timezone) + daily lifecycle + closed-loop inventory, on the validated sims.
   - Channel: check `m8trx-shared/brainstorm/COMMS-CONNECT-TWIN-2026-06-27.md` at session-start (C1✅ C2✅ resolved; C3 = §9 loop; OI-1/OI-2 core-side).
2. **Full activity — the "play"** (`ACTIVITY-PLAN.md`) — realized **via Connect + simulators** (#1): traffic → transactions → try-on → staff shifts/journeys → restock/stocktake (**BOH gives it a from-location**) → LP/EAS; item-movement is the connective tissue. The Kotlin Layer-0..3 generators feed the simulators. **Animate Wave 1 + light the analytics before expanding.**
3. **Close static-seed gaps via Connect** (Session 8 audit) — staff/org provisioning (candidate **TWIN-REQ-003**, hold for the API doc), per-region currency + localized names, sensor/reader topology (settle the zones-vs-readers event model), LP/EAS substrate (watch SKU source).
4. **Spatial Pass 2 — site assembly** (when calibration/placement data exists): fill `srf_to_site_transform` + designate `site_frame_anchor_space` + wire `space_connection` adjacency (FR-SPATIAL-26) for a unified site view + cross-space routing. Columns already in schema/emitted dormant → zero rework.
5. **Resolve image pipeline** with backend — link vs cached bytes (parallel).
6. **Wave 2 — 10 international stores** (`EXPANSION-PLAN.md`) — parametric per-store layout mechanism landed early; China←KR catalog, rest←US Shopify; onboard via Connect.

## Active Requirements Filed to Core

| Brief | Status | Blocks |
|-------|--------|--------|
| TWIN-REQ-001 `fitting_room` → `try_on_zone` | ✅ ABSORBED (mig 127) | — |
| TWIN-REQ-002 `commerce_projection` writer | 📨 FILED, AWAITING ABSORPTION (2026-06-11) | Scripts 1, 3, 5 |
| CORE-REQ-001 catalog attribute enrichment (**inverse, core→twin**) | ✅ ABSORBED 2026-06-21 (core merged-commit `eb39526`; mother loaded + verified) | — |
| CORE-REQ-002 `site_category` functional role (**inverse, core→twin**) | ✅ LIVE on mother (RE-RESEED v2, 2026-06-26; core mig 146) | — |
| CORE-REQ-003 build Connect simulators (**inverse, core→twin**) | ✅ **DONE (S11, 2026-06-30)** — all 5 P0 sims exercised END-TO-END live (inbound sales+catalog/pricing/shipment · Bearer self-verify closed loop · C3 outbound loop happy+retry/heal); 6 live `connect*` drivers | — |
| CORE-REQ-004 toolchain assessment (**inverse, core→twin**) | ✅ DONE 2026-06-29 (GO; PR #1 merged — full stack current, P0 jackson CVE cleared); deliverable `status/briefs/TWIN-TOOLCHAIN-ASSESSMENT-CORE-REQ-004-2026-06-29.md` | — |
| `inventory:sell` capability split | PRE-EXISTING in CLEANUP-TASKS | Cashier persona |
| **TWIN-REQ-005 — Connect capability acquisition** | 📨 **FILED 2026-07-31** (`m8trx-shared/twin/requirements/TWIN-REQ-005-connect-capability-acquisition.md`). No Connect key can obtain a capability it was not minted with, and §7's documented path (`PATCH …/service-keys/{keyId}/scopes`) is `CONNECT_NOT_EXPOSED` to every Connect key. Two individually-correct mechanisms (the admin-only door + SEC-1's subset guard) that together leave **no path at all**. Twin NOT blocked — twin asks a human, which is exactly what a real integrator cannot do. | `/alerts/query` uncallable by anyone; 2nd instance after `vision_ai:*` |
| **FR-INTEG-16 — two-system reconciliation** (POS + ERP for one EPC) — *feasibility asked of twin* | ✅ **ANSWERED 2026-07-31: YES.** Four steps, three already live-proven — `connectSaleStream` (POS), `connectMovementDrive` (ERP adjustment), HMAC signing. Step 1 is `POST /integrations` + `ChannelConfig(direction=inbound, endpoint_config={hmac_secret})`, currently **403 `PERMISSION_DENIED`** — a **scope** gap, restorable with one grant. ★ Because `endpointConfig` lets twin set `hmac_secret` **at creation**, the acceptance case **never needs a key mint**, so it never touches the permanently-closed `CONNECT_NOT_EXPOSED` key surface. **Bob's MVP ruling is safe on twin's side.** | one scope grant |
| **TWIN-REQ-004 — Connect READ surface** (task read · space/zone read · impression read) | ✅ **SATISFIED — CLOSED 2026-07-31 by twin as the external prover.** §6.5 shipped PR #210; typed 403s + the `/compliance/state` capability fix in PR #221 (both twin findings F1/F2, re-verified by twin rather than inherited — the F2 fix touched the 403 path the whole confinement verdict rests on, and confinement came back unchanged). **Acceptance gate satisfied in full:** site-scope confinement proven on **all four** reads with negative controls, by slug **and** by UUID, plus omitted-site returning **1 of 14** sites. Refusal semantics typed on every path (`SITE_ACCESS_DENIED` / `SITE_NOT_FOUND` / `DIRECTIVE_NOT_FOUND` / `INVALID_REQUEST`, and `200 count:0` held distinct from 404). **All three §4 interim positions retired with evidence:** fixture-map CSV → `/spatial/identity` (slug→UUID in one call); human `psql` → **3,119 impressions reproduced in code**, twice, at two slice sizes; engineer-watched task smoke → `/tasks/query`. Results: `m8trx-shared/status/active/RESULTS-TWIN-2026-07-31.md`. **Open, NOT blocking:** F6 — `/compliance/state` is UUID-only where its three siblings take a slug (BW-CONNECT's). Deliberately not treated as a blocker: the brief's complaint was an identifier **nothing on the surface returned**; F6's UUID *is* returned by `/spatial/identity` from a ref the integrator already knows, so it is an extra round trip, not a wall. | — |
| TWIN-REQ-003 — compliance/directive read-back (Connect Mode-3) | ✅ **SATISFIED** (2026-07-02; core PR #76 `925f9a4` deployed — `POST /api/v2/compliance/state` `@ConnectExposed`; twin part-3 smoke GREEN 28/28 · 25/2/1 · 0.893; `m8trx-shared/twin/requirements/TWIN-REQ-003-connect-compliance-readback.md`) | — |

## Key Docs
- Project context: `CLAUDE.md`
- Sister project contract: `~/IdeaProjects/m8trx-shared/twin/SISTER-PROJECT.md`
- Layer 4 schema: `reference/architecture/LAYER4-CONFIG-SCHEMA.md`
- Store layout: `reference/data/STORE-LAYOUT.md`
- API surface: `reference/integration/M8TRX-API-SURFACE.md`
