# Session 14 (continued, 2026-07-03) — CORE-REQ-005 part-3 · TWIN-REQ-003 satisfied · seat/perms finding · Strand V site-scope hardening

> Continues `2026-07-02-session-14-core-req-005-fullloop-compose-stress.md` (parts 1+2). Same session; ran far past the early close. Coordinator relayed by Bob (twin off the live comms-monitor per Bob's call).

## 1. CORE-REQ-005 part-3 — compliance read-back smoke GREEN → TWIN-REQ-003 SATISFIED
Core shipped **m8trx-services PR #76** (`feat/twin-compliance-readback`, merged `925f9a4`): `POST /api/v2/compliance/state` `{directive_ref, site_ref?}`, `@ConnectExposed(dataPlane=true)`, reuses `inventory:read` (no re-mint). Twin fired it live over the service Bearer → **HTTP 200**, directive `PLN-dec-us-denver-fe44dd290a92`: `activated:true`, 28/28 resolved+evaluated, summary **25/2/1**, `compliance_score 0.893` — matches core's validation to the decimal. **The S13/S198 `403 CONNECT_NOT_EXPOSED` wall is gone; the assert-split is closed** (twin can now self-verify compliance OUTPUT over the front door). TWIN-REQ-003 flipped **FILED → SATISFIED** (coord updated the brief; `absorbed-into-sprint: PR #76`). Caveat: `status`/`activation_status` not lifecycle-wired yet — use the top-level `activated` flag.

## 2. Seat / permissions finding (surfaced provisioning demo logins) — CORE's seed bug, twin clean
Provisioning shared demo logins (pw `Decathlon2026`, all 250 `@decathlon-demo.com`) surfaced that **every employee resolved tenant-wide** — `andrew.wilson` (Denver *site*-manager) roamed all 14 stores, ~99 caps at `TENANT` scope. Root cause **A (dominant):** `status/active/m8demo-seed/scripts/seed_users.py` (core/coord seed) inserts a tenant-level `user_tenant_membership` with the role profile for *every* employee → a tenant-wide baseline that shadows the (correct) site-scoped grants. **Twin self-audit = clean:** `users.csv` has exactly 1 `tenant_admin` (the operator), all 250 staff carry their `site`, and `build_staff_roster.py` **emits no capability/scope** ("backend maps them"). Two cosmetic twin nits noted for a future pass: the CSVs are **CRLF** (corrupts the trailing `tenant_admin` col) and the "provisioning-ready, tenant scope" comment reads ambiguously. Confirmed NOT a twin cause; Bob's read ("it was the way we seeded it") holds. The demo users were also **seeded credential-less** originally (`seed_users.py` wrote the user row + memberships, never a `user_identity`) — hence the shared-pw provisioning.

## 3. Strand V — site-scope confinement + stress harness (SECHARDEN) — the headline
Built **`connectSiteScopeAudit`** (branch `feature/connect-sitescope-audit`, **PR #11 merged** `e390e0f`; main `2131384`): a coordinator-invocable, re-runnable acceptance gate. **Public-surface + user-JWT only — no psql** (cohort-shaping + write-probe throwaway targets stayed coord/core-side, twin HARD RULE held throughout).
- **Files:** `UserAuthClient` (email+pw → JWT + decoded Hasura site-claim signal) · `HasuraClient` (aggregate-count reads as a user JWT) · `sim/SiteScopeAuditDriver` (token / store-picker / cross-site-read planes + gated writes + scale) · `ConnectSiteScopeAudit` entrypoint + gradle task. Env: `M8TRX_AUDIT_PASSWORD` (req), `M8TRX_HASURA_URL`, `M8TRX_AUDIT_SCALE`, `M8TRX_AUDIT_WRITE_TARGETS`.
- **Baseline (pre-fix): 24 RED** — JWT carried `x-hasura-tenant-id` only (no site claim); picker 14/14; cross-site reads leaked thousands (`item` 9507 Seoul / 14642 NYC / 4996 Bordeaux, plus `space`/`zone`/`scan_event`) for **both** andrew (Denver) and anais (Bordeaux).
- **Post-hardening (core S207, Strands 1/2/3/3b): 0 RED** — site claim present (andrew caps 111→96), picker 1/1, every cross-site read → 0, home reads intact (no over-deny).
- **★ The win — the at-scale/external gate caught 3 leaks the internal mapping missed.** A broad introspection sweep of **all 151 Hasura tables** (62 carry `site_id`) found 3 still leaking (28 Seoul rows each, visible to a Denver mgr): **`integration_event`, `item_custody_event`, `unified_audit_event`** — event/audit tables outside core's core-36/tail-35 coverage. Core added the predicate → twin re-verified **0**. Deeper nested-only + recursive-path sweeps closed the rest (`shipment`/`transfer_request` via `*_site_id`; VisionAI `crossing_line_event` via `crossing_line.space.site_id`; AR `anchor_health` via `ar_anchor.space.site_id`; `thing`-family via `items`) → **0 confirmed cross-site read leaks across all 151 tables**; ~55 tenant/user/global tables correctly not site-scoped.
- **Write plane (confused-deputy, coord `515c0be5-` throwaway targets):** as andrew — cross-site PSet-assign / tenant-role / task claim+start+complete / `compliance/state` all **403** (no write landed); `rules/fire{Seoul}` confirmed **403** by coord (Seoul 403 / Denver 200). Own-site PSet/tenant-role **403** = **design-correct** (a site-manager lacks `user:update` / `permission_set:assign` caps — correct capability denial, not a site over-deny).
- **Verdict: Strand V FULLY GREEN** — reads clean (151 tables), writes confined. Results doc (coord-side): `m8trx-shared/working-doc-archive/strand-twin-s014-results-2026-07-03.md`.

## Discoveries / decisions
- **Force-function proven again:** an independent, at-scale, external-integrator gate catches what internal domain-by-domain mapping misses (the 3 event/audit tables). This is the core value of the twin harness.
- **Perms root is the seed granting tenant-membership for site roles** (fix = make the seed mirror the invite flow; the roster already encodes HQ/region/store scope via the `site` column). Core's to fix.
- Posture held: all probing was public REST + Hasura-read + user-JWT; every mutating/psql step stayed coordinator-side.

## Branch / deploy state at close
- **m8trx-twin main `2131384`** — PRs #9 (fullloop) · #10 (stress) · #11 (sitescope) all merged. Clean tree.
- **TWIN-REQ-003:** SATISFIED (core PR #76 `925f9a4` deployed). **No new twin-reqs** this arc.
- Strand V arc **closed** (coord). Site-scope hardening green on M8trxDemo.
